# Generado para builds con ofuscación ligera (no commitear secretos re-empaquetados)
bridge_runtime_constants.py

# Icono generado en build (si no usas descarga.ico propio)
assets/bridge_app.ico

# PyInstaller (si lo usas a mano)
build/pyinstaller/
*.spec.bak

# Nuitka (cachés junto al proyecto; el nombre sigue al .py principal)
*.build/
*.dist/
*.onefile-build/
nuitka-crash-report.xml

# Salida local de empaquetado (opcional)
# DIST/
